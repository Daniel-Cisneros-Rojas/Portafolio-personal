import { featuredProjects as registryFeaturedProjects, projectBySlug as registryProjectBySlug, projects as registryProjects } from '../data/project-registry'

export const projects = registryProjects
export const projectBySlug = registryProjectBySlug
export const featuredProjects = registryFeaturedProjects
